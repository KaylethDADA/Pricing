worker_processes 4;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api {
            proxy_pass http://pricing.api:81;
        }
    }
}

     # location / {
     #      proxy_pass http://testexpo:8081;
     # }
